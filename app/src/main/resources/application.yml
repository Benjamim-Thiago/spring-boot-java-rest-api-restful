spring:
  application:
    name: app
  datasource:
    url: jdbc:postgresql://localhost:5432/course-spring-boot
    username: docker
    password: docker
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    show-sql: false